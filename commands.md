commands for git

// to create a new branch
git checkout -b branch_name


// to push to a branch
git push -u origin branch_name

// to add a file
git add file_path

// to add all files
git add .

make a ruleset for a coding ai agent for vibe coding to:
not add much debug codes and debug comments
and no unnecessary documentation and files, well structured files without permission and other vibe coding essentials and more